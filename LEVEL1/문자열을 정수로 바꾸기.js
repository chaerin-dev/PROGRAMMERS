function solution(s) {
  return Number(s);
}

/* 좋았던 다른 사람의 풀이:
꼭 더 좋은 풀이라기보다는 Number와의 사칙연산을 통해
자동으로 형 변환이 되는 점을 이용한 것이 인상깊었다.

function strToInt(str){
  return str/1;
  // 또는 return +str;
  // 또는 return str-0; 등...
}
*/
